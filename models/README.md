# Models
Trained weights, configurations, and associated evaluation data.

- Each model resides in its own subdirectory (`models/name/`).
- Include a minimal `config.json` and a `README.md` describing the model’s scope and training context.
- Evaluation outputs (e.g., benchmarks, logs) go in `models/name/eval/`.
